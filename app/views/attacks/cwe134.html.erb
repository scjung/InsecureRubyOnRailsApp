<% provide(:title, @title) %>
<h1><%= @title %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(url: attack_path(134), method: :patch, local: true) do |f| %>
      <div class="form-group">
        <%= f.label :label do %>
          Input a label:
        <% end %>
        <%= f.text_field :label, class: 'form-control', placeholder: 'label' %>
        <span class="help-block">
          <p>Sample input: The user name is </p>
        </span>
        <span class="help-block">
          <p>Malicious input: <%= '%<password>s' %></p>
        </span>
      </div>

      <div class="checkbox">
        <%= f.label :defense do %>
          <%= f.check_box :defense %>
          Defense the attack
        <% end %>
      </div>

      <%= f.submit 'Submit', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>

<% if @result %>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= @result %>
    </div>
  </div>
<% end %>